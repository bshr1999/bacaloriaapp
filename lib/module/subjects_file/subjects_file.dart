import 'package:flutter/material.dart';

class SubjectsFile extends StatelessWidget {
  const SubjectsFile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
